apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: adguard-stage-tls
  namespace: security
spec:
  secretName: adguard-stage-tls
  issuerRef:
    name: letsencrypt-production
    kind: ClusterIssuer
  dnsNames:
    - adguard-stage.burntbytes.com
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: audio-books-stage-tls
  namespace: security
spec:
  secretName: audio-books-stage-tls
  issuerRef:
    name: letsencrypt-production
    kind: ClusterIssuer
  dnsNames:
    - audio-books-stage.burntbytes.com
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: links-stage-tls
  namespace: security
spec:
  secretName: links-stage-tls
  issuerRef:
    name: letsencrypt-production
    kind: ClusterIssuer
  dnsNames:
    - links-stage.burntbytes.com
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: mealie-stage-tls
  namespace: security
spec:
  secretName: mealie-stage-tls
  issuerRef:
    name: letsencrypt-production
    kind: ClusterIssuer
  dnsNames:
    - mealie-stage.burntbytes.com
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: authelia-stage-tls
  namespace: security
spec:
  secretName: authelia-stage-tls
  issuerRef:
    name: letsencrypt-production
    kind: ClusterIssuer
  dnsNames:
    - auth.stage.burntbytes.com
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: golinks-stage-tls
  namespace: security
spec:
  secretName: golinks-stage-tls
  issuerRef:
    name: letsencrypt-production
    kind: ClusterIssuer
  dnsNames:
    - go.stage.burntbytes.com
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: biometrics-stage-tls
  namespace: security
spec:
  secretName: biometrics-stage-tls
  issuerRef:
    name: letsencrypt-production
    kind: ClusterIssuer
  dnsNames:
    - bio.stage.burntbytes.com
