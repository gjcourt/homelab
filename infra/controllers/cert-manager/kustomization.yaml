apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - repository.yaml
  - release.yaml

configMapGenerator:
  - name: cert-manager-helm-values
    namespace: security
    files:
      - values.yaml

configurations:
  - kustomizeconfig.yaml
