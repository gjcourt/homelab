# IMPORTANT: Replace with real credentials and encrypt with SOPS
# sops -e -i secret-linkding-container-env.yaml
apiVersion: v1
kind: Secret
metadata:
  name: linkding-container-env-secret
  namespace: linkding
type: Opaque
data:
  # LD_DB_PASSWORD will be injected from CNPG secret
  LD_DB_PASSWORD: REPLACE_WITH_BASE64_ENCODED_PASSWORD
