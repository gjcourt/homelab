apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: vitals-prod
resources:
  - ../../base/vitals/
  - configmap.yaml
  - database.yaml
  - httproute.yaml
  - secret-aws-creds.yaml
  - secret-db-credentials.yaml

labels:
  - pairs:
      env: production
      app.kubernetes.io/instance: production
    includeSelectors: false

patches:
  - target:
      kind: Namespace
      name: vitals
    patch: |
      - op: replace
        path: /metadata/name
        value: vitals-prod
