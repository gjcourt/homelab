apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: adguard-config-staging
  namespace: adguard
spec:
  accessModes: ["ReadWriteOnce"]
  storageClassName: synology-iscsi
  resources:
    requests:
      storage: 1Gi
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: adguard-work-staging
  namespace: adguard
spec:
  accessModes: ["ReadWriteOnce"]
  storageClassName: synology-iscsi
  resources:
    requests:
      storage: 1Gi
