apiVersion: v1
kind: Service
metadata:
  labels:
    app: linkding
  name: linkding
  namespace: linkding
  # annotations:
  #   metallb.io/allow-shared-ip: homelab
spec:
  ports:
    - name: http-web
      port: 9090
      # port: 9001
      # protocol: TCP
      # targetPort: 9090
  selector:
    app: linkding
  # type: LoadBalancer
  type: ClusterIP