# Database credentials for Immich
# Encrypt with SOPS before committing
apiVersion: v1
kind: Secret
metadata:
  name: immich-db-credentials
  namespace: immich-prod
type: kubernetes.io/basic-auth
stringData:
  username: immich
  password: changeme-immich-db-password
