apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: memos-data-pvc
  namespace: memos
  labels:
    app: memos
spec:
  storageClassName: truenas-iscsi
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
