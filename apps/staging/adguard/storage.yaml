apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: adguard-config-stage
  namespace: adguard-stage
spec:
  accessModes: ["ReadWriteOnce"]
  storageClassName: synology-iscsi
  resources:
    requests:
      storage: 1Gi
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: adguard-work-stage
  namespace: adguard-stage
spec:
  accessModes: ["ReadWriteOnce"]
  storageClassName: synology-iscsi
  resources:
    requests:
      storage: 1Gi
