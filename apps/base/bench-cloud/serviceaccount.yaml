# Identity for both the console and task Jobs. Its only cluster permission is
# the built-in read-only `view` role (rbac.yaml).
apiVersion: v1
kind: ServiceAccount
metadata:
  name: bench-agent
  namespace: bench-cloud
