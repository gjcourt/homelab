apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mealie-data-pvc
  namespace: mealie
  labels:
    app: mealie
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 200Mi