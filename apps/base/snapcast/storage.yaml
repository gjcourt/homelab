apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: snapcast-spotify-state
  namespace: snapcast
  labels:
    app: snapcast
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
