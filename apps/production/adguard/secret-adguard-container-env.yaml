# IMPORTANT: Replace with real credentials and encrypt with SOPS
# sops -e -i secret-adguard-container-env.yaml
apiVersion: v1
kind: Secret
metadata:
  name: adguard-container-env-secret
  namespace: adguard
type: Opaque
data:
  # Add any sensitive adguard configuration here
  PLACEHOLDER: UExBQ0VIT0xERVI=
