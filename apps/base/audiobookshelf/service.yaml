apiVersion: v1
kind: Service
metadata:
  name: audiobookshelf
  namespace: audiobookshelf
  annotations:
    metallb.io/allow-shared-ip: homelab
  labels:
    app: audiobookshelf
type: ClusterIP
spec:
  ports:
    - name: http-web
      port: 3005
      protocol: TCP
      targetPort: 13378
  selector:
    app: audiobookshelf