apiVersion: v1
kind: Service
metadata:
  name: kube-prometheus-stack
  namespace: kube-prometheus-stack
  labels:
    app: kube-prometheus-stack
spec:
  type: ClusterIP
  ports:
    - name: http-web
      port: 8080
      protocol: TCP
      targetPort: 80
  selector:
    app: kube-prometheus-stack