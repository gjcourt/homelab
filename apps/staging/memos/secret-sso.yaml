apiVersion: v1
kind: Secret
metadata:
  name: memos-sso-secret
  namespace: memos-stage
  labels:
    app: memos
    env: staging
type: Opaque
stringData:
  # TODO: Encrypt this file with SOPS before merging.
  # This secret stores the Memos OIDC client_secret for reference.
  # The actual secret is configured in the Memos admin UI (Settings → SSO).
  # Authelia has the pbkdf2-sha512 hashed version in its configuration.
  client_secret: "PLACEHOLDER"
