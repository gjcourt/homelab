apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: snapcast-stage
resources:
  - ../../base/snapcast/
  - httproute.yaml

labels:
  - pairs:
      env: staging
      app.kubernetes.io/instance: staging
    includeSelectors: false

patches:
  - target:
      kind: Namespace
      name: snapcast
    patch: |
      - op: replace
        path: /metadata/name
        value: snapcast-stage
