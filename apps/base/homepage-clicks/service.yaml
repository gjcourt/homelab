apiVersion: v1
kind: Service
metadata:
  name: homepage-clicks
  namespace: homepage
  labels:
    app.kubernetes.io/name: homepage-clicks
    app.kubernetes.io/component: exporter
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: homepage-clicks
    app.kubernetes.io/component: exporter
  ports:
    - name: metrics
      port: 9107
      targetPort: metrics
      protocol: TCP
