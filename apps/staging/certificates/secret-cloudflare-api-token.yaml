apiVersion: v1
kind: Secret
metadata:
  name: cloudflare-api-token
  namespace: security
type: Opaque
stringData:
  # Create a Cloudflare API token with DNS:Edit permissions for burntbytes.com
  # Then encrypt this value using: scripts/encrypt-secret.sh
  api-token: REPLACE_WITH_CLOUDFLARE_API_TOKEN
