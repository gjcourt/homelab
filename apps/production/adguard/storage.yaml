apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: adguard-config-production
  namespace: adguard
spec:
  accessModes: ["ReadWriteOnce"]
  storageClassName: synology-iscsi
  resources:
    requests:
      storage: 1Gi
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: adguard-work-production
  namespace: adguard
spec:
  accessModes: ["ReadWriteOnce"]
  storageClassName: synology-iscsi
  resources:
    requests:
      storage: 1Gi
