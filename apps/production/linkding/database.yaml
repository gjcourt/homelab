apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: linkding-db-production-cnpg-v1
  namespace: linkding

spec:
  description: Postgres cluster for the linkding application (production)
  imageName: ghcr.io/cloudnative-pg/postgresql:17.5

  instances: 3

  inheritedMetadata:
    labels:
      app: linkding-database
      env: production
      policy-type: "database"

  monitoring:
    enablePodMonitor: true

  storage:
    size: 10Gi

  backup:
    barmanObjectStore:
      destinationPath: s3://gjcourt-homelab-backup/production/linkding
      s3Credentials:
        accessKeyId:
          name: linkding-aws-creds-secret
          key: ACCESS_KEY_ID
        secretAccessKey:
          name: linkding-aws-creds-secret
          key: ACCESS_SECRET_KEY
      wal:
        compression: gzip
      data:
        compression: gzip
    retentionPolicy: "30d"

  bootstrap:
    initdb:
      database: app
      owner: app
