apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: flashcards-stage
resources:
  - ../../base/flashcards/
  - httproute.yaml

labels:
  - pairs:
      env: staging
      app.kubernetes.io/instance: staging
    includeSelectors: false

patches:
  - target:
      kind: Namespace
      name: flashcards
    patch: |
      - op: replace
        path: /metadata/name
        value: flashcards-stage
