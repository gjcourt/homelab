apiVersion: v1
kind: Service
metadata:
  labels:
    app: audiobookshelf
  name: audiobookshelf
  namespace: audiobookshelf
  annotations:
    metallb.io/allow-shared-ip: homelab
spec:
  ports:
    - name: http-web
      port: 3000
  selector:
    app: audiobookshelf
  # type: LoadBalancer
  type: ClusterIP