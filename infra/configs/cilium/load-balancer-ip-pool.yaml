apiVersion: cilium.io/v2
kind: CiliumLoadBalancerIPPool
metadata:
  # Named home-c-pool to match lbipam.cilium.io/ip-pool annotations across
  # gateway and LoadBalancer service resources in this cluster.
  name: home-c-pool
spec:
  blocks:
    - start: 10.42.2.40
      stop: 10.42.2.254
