apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: openwebui
  namespace: openwebui
  labels:
    app: openwebui
spec:
  maxUnavailable: 0
  selector:
    matchLabels:
      app: openwebui
