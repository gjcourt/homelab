apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - release.yaml
  - repository.yaml
  - secret-client-info.yaml

configMapGenerator:
  - name: synology-csi-helm-values
    namespace: synology-csi
    files:
      - values.yaml

configurations:
  - kustomizeconfig.yaml
