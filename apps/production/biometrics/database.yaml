apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: biometrics-db-production-cnpg-v1
  namespace: biometrics-prod
spec:
  description: Postgres cluster for the biometrics application (production)
  imageName: ghcr.io/cloudnative-pg/postgresql:17.5
  instances: 3
  inheritedMetadata:
    labels:
      app: biometrics-database
      env: production
      policy-type: "database"
  monitoring:
    enablePodMonitor: true
  storage:
    size: 10Gi
  # TODO: Re-enable backup once real S3 credentials are configured
  # backup:
  #   barmanObjectStore:
  #     destinationPath: s3://gjcourt-homelab-backup/production/biometrics
  #     s3Credentials:
  #       accessKeyId:
  #         name: biometrics-aws-creds-secret
  #         key: ACCESS_KEY_ID
  #       secretAccessKey:
  #         name: biometrics-aws-creds-secret
  #         key: ACCESS_SECRET_KEY
  #     wal:
  #       compression: gzip
  #     data:
  #       compression: gzip
  #   retentionPolicy: "30d"
  bootstrap:
    initdb:
      database: app
      owner: app
      secret:
        name: biometrics-db-credentials
  # TODO: Re-enable externalClusters once real S3 credentials are configured
  # externalClusters:
  #   - name: biometrics-db-backup
  #     barmanObjectStore:
  #       destinationPath: s3://gjcourt-homelab-backup/production/biometrics
  #       s3Credentials:
  #         accessKeyId:
  #           name: biometrics-aws-creds-secret
  #           key: ACCESS_KEY_ID
  #         secretAccessKey:
  #           name: biometrics-aws-creds-secret
  #           key: ACCESS_SECRET_KEY
