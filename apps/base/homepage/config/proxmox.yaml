# Required by homepage v1.10+ as a config skeleton file.
# If absent at startup, the app tries to copy it from the read-only skeleton
# dir into the ConfigMap-backed /app/config mount, which fails (EACCES) and
# causes a crash-loop. Keep this file present even if empty.
