apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: adguard-stage-tls
  namespace: security
spec:
  secretName: adguard-stage-tls
  issuerRef:
    name: letsencrypt-production
    kind: ClusterIssuer
  dnsNames:
    - adguard-stage.burntbytes.com
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: audio-books-stage-tls
  namespace: security
spec:
  secretName: audio-books-stage-tls
  issuerRef:
    name: letsencrypt-production
    kind: ClusterIssuer
  dnsNames:
    - audio-books-stage.burntbytes.com
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: links-stage-tls
  namespace: security
spec:
  secretName: links-stage-tls
  issuerRef:
    name: letsencrypt-production
    kind: ClusterIssuer
  dnsNames:
    - links-stage.burntbytes.com
  # Force DNS-01 challenge by requesting a wildcard or using solver selection
  # The ClusterIssuer will automatically use DNS-01 based on the dnsNames selector
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: mealie-stage-tls
  namespace: security
spec:
  secretName: mealie-stage-tls
  issuerRef:
    name: letsencrypt-production
    kind: ClusterIssuer
  dnsNames:
    - mealie-stage.burntbytes.com
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: grafana-tls
  namespace: security
spec:
  secretName: grafana-tls
  issuerRef:
    name: letsencrypt-production
    kind: ClusterIssuer
  dnsNames:
    - grafana.burntbytes.com
