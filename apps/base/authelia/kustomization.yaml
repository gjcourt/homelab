apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - storage.yaml
  - deployment.yaml
  - service.yaml
configMapGenerator:
  - name: authelia-config
    namespace: authelia
    files:
      - configuration.yml
