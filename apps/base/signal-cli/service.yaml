apiVersion: v1
kind: Service
metadata:
  name: signal-cli-bridge
  namespace: signal-cli
  labels:
    app: signal-cli
spec:
  type: ClusterIP
  selector:
    app: signal-cli
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
