apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: flashcards-db-staging-cnpg-v1
  namespace: flashcards-stage
spec:
  description: Postgres cluster for the flashcards sync service (staging)
  imageName: ghcr.io/cloudnative-pg/postgresql:17.5
  instances: 3
  inheritedMetadata:
    labels:
      app: flashcards-database
      env: staging
      policy-type: "database"
  monitoring:
    enablePodMonitor: true
  storage:
    size: 2Gi
    storageClass: truenas-iscsi-ssd
  plugins:
    - name: barman-cloud.cloudnative-pg.io
      isWALArchiver: true
      parameters:
        barmanObjectName: flashcards-db-staging-cnpg-v1-backup
        serverName: flashcards-db-staging-cnpg-v1
  externalClusters:
    - name: flashcards-db-backup
      plugin:
        name: barman-cloud.cloudnative-pg.io
        parameters:
          barmanObjectName: flashcards-db-staging-cnpg-v1-backup
          serverName: flashcards-db-staging-cnpg-v1
  bootstrap:
    initdb:
      database: app
      owner: app
      secret:
        name: flashcards-db-credentials
