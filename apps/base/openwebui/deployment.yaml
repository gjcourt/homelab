apiVersion: apps/v1
kind: Deployment
metadata:
  name: openwebui
  namespace: openwebui
  labels:
    app: openwebui
spec:
  # Re-enabled 2026-09-16: backend repointed from the (removed) llama.cpp on
  # hestia to the Anthropic API via its OpenAI-compat layer.
  replicas: 1
  revisionHistoryLimit: 5
  selector:
    matchLabels:
      app: openwebui
  # RWO PVC requires Recreate — only one pod can mount at a time
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: openwebui
      annotations:
        # Nothing restarts this pod when openwebui-env changes (no Reloader, no
        # hashed ConfigMap), and env vars are only read at startup — so a
        # ConfigMap edit alone never takes effect. BUMP THIS VALUE in the same
        # PR as any configmap.yaml or secret change to roll the pod.
        homelab.burntbytes.com/config-revision: "2026-09-25-llmux-client-key"
    spec:
      serviceAccountName: openwebui
      automountServiceAccountToken: false
      securityContext:
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: openwebui
          # NOTE: this MUST be the multi-arch index digest, not a platform-specific
          # one. The cluster is amd64; resolving a digest via `podman inspect` on an
          # arm64 Mac yields the arm64 manifest and the pod dies with
          # `exec /usr/bin/bash: exec format error`. Get it with:
          #   podman manifest inspect ghcr.io/open-webui/open-webui:<tag>
          # or the registry's docker-content-digest header.
          image: ghcr.io/open-webui/open-webui:v0.11.4@sha256:9591b13f13843c7721c2b8eaf7382846c81b3ffe126526d1888d1fed50c6a33f
          ports:
            - containerPort: 8080
          envFrom:
            - configMapRef:
                name: openwebui-env
            # Operator-managed, NOT in git (see secret-anthropic.yaml.example).
            # optional: production has the funded key; staging deliberately does not,
            # so the preview environment starts without reaching a paid API.
            - secretRef:
                name: openwebui-anthropic
                optional: true
          volumeMounts:
            - name: data
              mountPath: /app/backend/data
          # startupProbe gates liveness/readiness while Open WebUI initializes.
          # Allows up to 5 minutes for first-run model loading.
          startupProbe:
            httpGet:
              path: /health
              port: 8080
            failureThreshold: 30
            periodSeconds: 10
            timeoutSeconds: 5
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 0
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 0
            periodSeconds: 20
            timeoutSeconds: 5
            failureThreshold: 6
          resources:
            requests:
              cpu: 50m
              memory: 256Mi
            limits:
              cpu: 1000m
              memory: 2Gi
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: openwebui-data-pvc
