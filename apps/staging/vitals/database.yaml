apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: vitals-db-staging-cnpg-v1
  namespace: vitals-stage
spec:
  description: Postgres cluster for the vitals application
  imageName: ghcr.io/cloudnative-pg/postgresql:17.5
  instances: 3
  inheritedMetadata:
    labels:
      app: vitals-database
      env: staging
      policy-type: "database"
  monitoring:
    enablePodMonitor: true
  # Cross-cluster anti-affinity: see apps/production/golinks/database.yaml
  # for full rationale. Soft (weight=50) preference to land new pods on
  # nodes that don't already host other CNPG postgres pods.
  affinity:
    additionalPodAntiAffinity:
      preferredDuringSchedulingIgnoredDuringExecution:
        - weight: 50
          podAffinityTerm:
            labelSelector:
              matchLabels:
                policy-type: database
            topologyKey: kubernetes.io/hostname
  storage:
    size: 2Gi
  # WAL archiving and base backups via the Barman Cloud Plugin (v0.11.0).
  # S3 configuration is defined in objectstore.yaml (ObjectStore CRD).
  plugins:
    - name: barman-cloud.cloudnative-pg.io
      isWALArchiver: true
      parameters:
        barmanObjectName: vitals-db-staging-cnpg-v1-backup
        serverName: vitals-db-staging-cnpg-v1
  bootstrap:
    initdb:
      database: app
      owner: app
      secret:
        name: vitals-db-credentials
  # externalClusters references the ObjectStore via the plugin for PITR/restore.
  externalClusters:
    - name: vitals-db-backup
      plugin:
        name: barman-cloud.cloudnative-pg.io
        parameters:
          barmanObjectName: vitals-db-staging-cnpg-v1-backup
          serverName: vitals-db-staging-cnpg-v1
