apiVersion: v1
kind: Secret
metadata:
  name: immich-sso-secret
  namespace: immich-stage
type: Opaque
stringData:
  # Placeholder for client secret
  # Run: sops -e -i secret-sso.yaml
  client_secret: "CHANGE_ME_TO_SECURE_RANDOM_STRING"
