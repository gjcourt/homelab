apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: vitals-db-staging-cnpg-v1
  namespace: vitals-stage
spec:
  description: Postgres cluster for the vitals application
  imageName: ghcr.io/cloudnative-pg/postgresql:17.5
  instances: 3
  inheritedMetadata:
    labels:
      app: vitals-database
      env: staging
      policy-type: "database"
  monitoring:
    enablePodMonitor: true
  storage:
    size: 2Gi
  # TODO: Re-enable backup once real S3 credentials are configured
  # backup:
  #   barmanObjectStore:
  #     destinationPath: s3://gjcourt-homelab-backup/staging/vitals/v1
  #     s3Credentials:
  #       accessKeyId:
  #         name: vitals-aws-creds-secret
  #         key: ACCESS_KEY_ID
  #       secretAccessKey:
  #         name: vitals-aws-creds-secret
  #         key: ACCESS_SECRET_KEY
  #     wal:
  #       compression: gzip
  #     data:
  #       compression: gzip
  #     serverName: vitals-db-staging-cnpg-v1
  #   retentionPolicy: 14d
  bootstrap:
    initdb:
      database: app
      owner: app
      secret:
        name: vitals-db-credentials
  # TODO: Re-enable externalClusters once real S3 credentials are configured
  # externalClusters:
  #   - name: vitals-db-backup
  #     barmanObjectStore:
  #       destinationPath: s3://gjcourt-homelab-backup/staging/vitals/v1
  #       s3Credentials:
  #         accessKeyId:
  #           name: vitals-aws-creds-secret
  #           key: ACCESS_KEY_ID
  #         secretAccessKey:
  #           name: vitals-aws-creds-secret
  #           key: ACCESS_SECRET_KEY
