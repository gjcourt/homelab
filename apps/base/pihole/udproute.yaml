apiVersion: gateway.networking.k8s.io/v1alpha2
kind: UDPRoute
metadata:
  name: pihole-dns-udp
  namespace: pihole
spec:
  parentRefs:
    - name: app-gateway-staging
      namespace: default
      sectionName: dns-udp
  rules:
    - backendRefs:
      - name: pihole
        port: 53