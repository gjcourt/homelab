apiVersion: apps/v1
kind: Deployment
metadata:
  name: linkding
  namespace: linkding-stage
spec:
  template:
    spec:
      hostAliases:
        - ip: "192.168.5.30"
          hostnames:
            - "auth.stage.burntbytes.com"
      containers:
        - name: linkding
          envFrom:
            - configMapRef:
                name: linkding-oidc-config
            - secretRef:
                name: linkding-oidc-secret