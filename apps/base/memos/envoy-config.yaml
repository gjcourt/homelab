apiVersion: v1
kind: ConfigMap
metadata:
  name: memos-envoy-config
  namespace: memos
data:
  envoy.yaml: |
    static_resources:
      listeners:
        - name: listener_0
          address:
            socket_address:
              address: 0.0.0.0
              port_value: 8080
          filter_chains:
            - filters:
                - name: envoy.filters.network.http_connection_manager
                  typed_config:
                    "@type": type.googleapis.com/envoy.extensions.filters.network.http_connection_manager.v3.HttpConnectionManager
                    stat_prefix: ingress_http
                    codec_type: AUTO
                    route_config:
                      name: local_route
                      virtual_hosts:
                        - name: memos
                          domains: ["*"]
                          cors:
                            allow_origin_string_match:
                              - safe_regex:
                                  regex: ".*"
                            allow_methods: "GET, POST, PUT, DELETE, PATCH, OPTIONS"
                            allow_headers: "content-type, x-grpc-web, x-user-agent, grpc-timeout, authorization, cookie"
                            expose_headers: "grpc-status, grpc-message, set-cookie"
                            allow_credentials: true
                            max_age: "86400"
                          routes:
                            # Route gRPC-web requests to HTTP/2 cluster
                            - match:
                                prefix: "/"
                                headers:
                                  - name: content-type
                                    prefix_match: "application/grpc"
                              route:
                                cluster: memos_grpc
                            # Route everything else to HTTP/1.1 cluster
                            - match:
                                prefix: "/"
                              route:
                                cluster: memos_http
                    http_filters:
                      # gRPC-web filter to translate gRPC-web to gRPC
                      - name: envoy.filters.http.grpc_web
                        typed_config:
                          "@type": type.googleapis.com/envoy.extensions.filters.http.grpc_web.v3.GrpcWeb
                      # CORS filter for browser requests
                      - name: envoy.filters.http.cors
                        typed_config:
                          "@type": type.googleapis.com/envoy.extensions.filters.http.cors.v3.Cors
                      # Lua filter to rewrite grpc-metadata-set-cookie to set-cookie
                      - name: envoy.filters.http.lua
                        typed_config:
                          "@type": type.googleapis.com/envoy.extensions.filters.http.lua.v3.Lua
                          default_source_code:
                            inline_string: |
                              function envoy_on_response(response_handle)
                                local grpc_cookie = response_handle:headers():get("grpc-metadata-set-cookie")
                                if grpc_cookie then
                                  response_handle:headers():add("set-cookie", grpc_cookie)
                                  response_handle:headers():remove("grpc-metadata-set-cookie")
                                end
                              end
                      - name: envoy.filters.http.router
                        typed_config:
                          "@type": type.googleapis.com/envoy.extensions.filters.http.router.v3.Router

      clusters:
        # HTTP/2 cluster for gRPC requests
        - name: memos_grpc
          connect_timeout: 30s
          type: STATIC
          lb_policy: ROUND_ROBIN
          typed_extension_protocol_options:
            envoy.extensions.upstreams.http.v3.HttpProtocolOptions:
              "@type": type.googleapis.com/envoy.extensions.upstreams.http.v3.HttpProtocolOptions
              explicit_http_config:
                http2_protocol_options: {}
          load_assignment:
            cluster_name: memos_grpc
            endpoints:
              - lb_endpoints:
                  - endpoint:
                      address:
                        socket_address:
                          address: 127.0.0.1
                          port_value: 5230
        # HTTP/1.1 cluster for web UI and REST API
        - name: memos_http
          connect_timeout: 30s
          type: STATIC
          lb_policy: ROUND_ROBIN
          load_assignment:
            cluster_name: memos_http
            endpoints:
              - lb_endpoints:
                  - endpoint:
                      address:
                        socket_address:
                          address: 127.0.0.1
                          port_value: 5230
