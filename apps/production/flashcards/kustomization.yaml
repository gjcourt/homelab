apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: flashcards-prod
resources:
  - ../../base/flashcards/
  - httproute.yaml

labels:
  - pairs:
      env: production
      app.kubernetes.io/instance: production
    includeSelectors: false

patches:
  - target:
      kind: Namespace
      name: flashcards
    patch: |
      - op: replace
        path: /metadata/name
        value: flashcards-prod
