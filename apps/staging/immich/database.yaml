apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: immich-db-staging-cnpg-v1
  namespace: immich-stage
spec:
  description: Postgres cluster for Immich with VectorChord for ML search
  # Use Immich's official postgres image with VectorChord
  imageName: ghcr.io/immich-app/postgres:17-vectorchord0.4.3

  instances: 3

  inheritedMetadata:
    labels:
      app: immich-database
      env: staging
      policy-type: "database"

  monitoring:
    enablePodMonitor: true

  storage:
    size: 5Gi

  postgresql:
    shared_preload_libraries:
      - "vchord.so"

  backup:
    barmanObjectStore:
      destinationPath: s3://gjcourt-homelab-backup/staging/immich
      s3Credentials:
        accessKeyId:
          name: immich-aws-creds-secret
          key: ACCESS_KEY_ID
        secretAccessKey:
          name: immich-aws-creds-secret
          key: ACCESS_SECRET_KEY
      serverName: immich-db-staging-cnpg-v1
    retentionPolicy: 14d

  bootstrap:
    initdb:
      database: immich
      owner: immich
      secret:
        name: immich-db-credentials
      postInitSQL:
        - CREATE EXTENSION IF NOT EXISTS vchord CASCADE;
        - CREATE EXTENSION IF NOT EXISTS earthdistance CASCADE;

  externalClusters:
    - name: immich-db-backup
      barmanObjectStore:
        destinationPath: s3://gjcourt-homelab-backup/staging/immich
        s3Credentials:
          accessKeyId:
            name: immich-aws-creds-secret
            key: ACCESS_KEY_ID
          secretAccessKey:
            name: immich-aws-creds-secret
            key: ACCESS_SECRET_KEY
