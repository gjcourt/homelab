apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - deployment.yaml
  - namespace.yaml
  - service.yaml
  - servicemonitor.yaml
  - storage.yaml
configMapGenerator:
  - name: authelia-config
    namespace: authelia
    files:
      - configuration.yaml
